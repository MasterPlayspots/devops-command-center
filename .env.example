# ─── NICHT MEHR BENÖTIGT ──────────────────────────────
# VITE_API_BASE wird nicht mehr verwendet!
# Alle API-Calls gehen über den Pages Functions Proxy (/api/*)
# Die Worker-URL wird serverseitig über env.API_UPSTREAM konfiguriert.
#
# Zum lokalen Testen mit wrangler pages dev:
#   wrangler pages dev dist --binding API_UPSTREAM=cf-ai-workspace.ourark.workers.dev
